# Overload == Operator for Distance

* Create a class `Distance` with two private data members: `feet` and `inches`.
* Provide a constructor to initialize the values.
* Overload the `==` operator using a member function.
* Display whether two distances are equal or not.

| **Criteria**                         | **Excellent (3)**                                                                                                                                                  | **Good (2)**                                                                  | **Need Improvement (1)**                                            | **Poor (0)**                                                           |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| **Class Definition & Data Members**  | `Distance` class is correctly defined with private `feet` and `inches` data members and proper access structure.                                                   | Class and data members are mostly correct with minor syntax or design issues. | Some required members are missing or incorrectly defined/accessed.  | Class or required data members are not implemented.                    |
| **Constructor Implementation**       | Constructor correctly initializes both `feet` and `inches` values and is properly used when creating objects.                                                      | Constructor works with minor initialization or syntax issues.                 | Constructor is incomplete or values are not initialized correctly.  | Constructor is missing or completely incorrect.                        |
| **`==` Operator Overloading**        | `==` is correctly overloaded as a member function and accurately compares both distance values.                                                                    | Operator overloading is implemented with minor comparison or syntax issues.   | Operator is partially implemented or comparison logic is incorrect. | `==` operator overloading is not implemented.                          |
| **Object Usage, Output & Execution** | Two `Distance` objects are created, compared correctly, and a clear message indicates whether the distances are equal; program compiles and executes successfully. | Program mostly works with minor output or execution issues.                   | Comparison/output is incomplete or partially working.               | Program does not compile/run or required comparison output is missing. |

### **Plagiarism Policy**
* **Copied Code Check:** If the student's code is found to be plagiarized or matching 100% with another student's code, a flat penalty of **3 marks** will be deducted from their total score.