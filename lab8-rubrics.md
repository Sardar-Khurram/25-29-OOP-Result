# Shape Area Task

* Create base class `Shape` with `virtual double area()`
* Derive `Rectangle` and `Circle`
* Override `area()` to compute actual values
* Use `Shape*` pointer to access different shapes

| **Criteria**                            | **Excellent (3)**                                                                                                                         | **Good (2)**                                                                                   | **Need Improvement (1)**                                                                      | **Poor (0)**                                               |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| **Polymorphism & Virtual Function**     | `Shape` correctly defines a virtual `area()` function, and derived classes properly override it to demonstrate runtime polymorphism.      | Virtual function and overriding are mostly correct with minor syntax or implementation issues. | Virtual/override implementation is incomplete or polymorphism is only partially demonstrated. | Polymorphism or the virtual function is not implemented.   |
| **Derived Classes & Area Calculation**  | `Rectangle` and `Circle` are correctly derived from `Shape`, and both calculate their actual areas accurately.                            | Both derived classes are implemented with minor calculation or structural errors.              | One derived class or area calculation is incomplete/incorrect.                                | Required derived classes or area calculations are missing. |
| **Shape Pointer & Object Access**       | A `Shape*` pointer is correctly used to reference different derived objects and invoke the appropriate overridden `area()` function.      | Shape pointer is used correctly with minor object-access or function-call issues.              | Pointer-based access is partially implemented or does not fully demonstrate polymorphism.     | `Shape*` pointer is not used or is used incorrectly.       |
| **Object Creation, Output & Execution** | Rectangle and Circle objects are created correctly, results are displayed accurately, and the program compiles and executes successfully. | Program mostly works with minor output or execution issues.                                    | Program partially executes or output/calculations are incomplete or unclear.                  | Code does not compile/run or required output is missing.   |

### **Plagiarism Policy**
* **Copied Code Check:** If the student's code is found to be plagiarized or matching 100% with another student's code, a flat penalty of **3 marks** will be deducted from their total score.