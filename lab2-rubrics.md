# Lab 2

## Task 1: Student Record System (Functions Inside Class)
**Prepared by:** Engr. M.Awais 22

Create a class `Student` with the following attributes:
* `string name`
* `int rollNumber`
* `float marks`

Define the following member functions inside the class:
* `getData()` – to take input from the user
* `displayData()` – to display student information

Create an object and test the functionality.

---

## Task 2: Rectangle (Functions Outside Class)
**Prepared by:** Engr. M.Awais 23

Create a class `Rectangle` with attributes:
* `int length`
* `int width`

Define the following member functions outside the class:
* `setDimensions()` – to input length and width
* `area()` – to calculate area
* `perimeter()` – to calculate perimeter

Create an object and test the functionality.

---

## Task 3: Voting Eligibility
**Prepared by:** Engr. M.Awais 24

Create a class `Voter` with attributes:
* `string name`
* `int age`

Define the following member functions outside the class:
* `isEligible()` – to check the eligibility of the voter. (Only voter age > 18 is eligible). This method should return true or false based on the age of the person.
* Based on what the `isEligible()` method returns show an appropriate message to the user in main function.

Create an object and test the functionality.

---

## Task 4: Temperature Converter
**Prepared by:** Engr. M.Awais 25

Create a class named `Temperature` that converts temperature from Celsius to Fahrenheit. The class should have:
* A public data member to store temperature in Celsius.
* A function to convert the temperature using the formula.
* A function to display the converted value.

Create an object and test the functionality.

---

## Task 5: Calculator (Functions Outside Class)
**Prepared by:** Engr. M.Awais 26

Create a class `Calculator` with attributes:
* `float num1`
* `float num2`

Define the following member functions inside or outside the class:
* `add()`
* `subtract()`
* `multiply()`
* `divide()`

Allow the user to input two numbers and choose an operation to perform.

---

### **Evaluation Rubric**

| **Criteria**                          | **Excellent (3)**                                            | **Good (2)**                                          | **Need Improvement (1)**                  | **Poor (0)**                             |
| ------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------- | ----------------------------------------- | ---------------------------------------- |
| **Class Definition & Object Usage**   | Correct class structure with object creation; proper syntax. | Minor syntax issues but structure is mostly correct.  | Major issues in class or object usage.    | Not attempted / completely incorrect.    |
| **Member Variables**                  | Relevant attributes defined clearly with proper data types.  | Attributes defined with minor issues.                 | Attributes poorly defined or unclear.     | No meaningful use of member variables.   |
| **Member Functions (Inside/Outside)** | Functions defined and used correctly as per instruction.     | Functions defined with small syntax/placement errors. | Poor function structure or logic missing. | No functions defined or irrelevant code. |
| **Input/Output & Execution**          | Takes input and displays output correctly and clearly.       | Input/output mostly works with small issues.          | Output unclear or partially working.      | Code does not run or no input/output.    |

---

### **Plagiarism Policy**
* **Copied Code Check:** If the student's code is found to be plagiarized or matching 100% with another student's code, a flat penalty of **5 marks** will be deducted from their total score.
