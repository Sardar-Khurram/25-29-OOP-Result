# Tasks

### **Task 1**
* Declares a structure named `Student` to store:
  * First Name
  * Last Name
  * Roll Number
  * Marks
* Includes a member function `displayStudentInfo()` to:
  * Display the full name of the student.
  * Display the marks of the student.
* Uses a structure variable to:
  * Assign values to the data members.
  * Call the member function to display the student's information.

**Prepared By : M.Awais 10**

---

### **Task 2**
* Repeat the previous program, but this time:
  * Use an array of structures to store details of multiple students.
  * Allow the user to input details for multiple students.
  * Call the `displayStudentInfo()` function for each student to display their full name and marks.

---

### **Task 3**
* Modify your previous program to:
  * Use a pointer to a structure to store and access student details.
  * Dynamically assign values to the structure members using the pointer.
  * Use the pointer to call the `displayStudentInfo()` function

| **Criteria**                               | **Excellent (3)**                                                                                                                                    | **Good (2)**                                                                          | **Need Improvement (1)**                                                       | **Poor (0)**                                                             |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------ |
| **Structure Definition & Member Function** | `Student` structure is correctly defined with all required members, and `displayStudentInfo()` correctly displays the student's full name and marks. | Structure and function are mostly correct with minor syntax or implementation issues. | Some members or function requirements are missing or incorrectly implemented.  | Structure or member function is not implemented or completely incorrect. |
| **Array of Structures**                    | Array of structures is correctly implemented; multiple student records are accepted and displayed using the member function.                         | Array implementation works with minor input, indexing, or display issues.             | Array of structures is partially implemented or only handles limited records.  | Array of structures is not implemented.                                  |
| **Structure Pointer & Dynamic Access**     | Structure pointer is correctly declared and used to dynamically assign/access members and call `displayStudentInfo()`.                               | Pointer implementation is mostly correct with minor syntax or access issues.          | Pointer is used but member access or function calling is incomplete/incorrect. | Structure pointer is not implemented or used incorrectly.                |
| **Input, Output & Execution**              | Programs compile and execute correctly; all required student data is assigned/input and displayed clearly for all tasks.                             | Programs mostly execute correctly with minor input, output, or execution issues.      | Programs partially execute or output is incomplete/unclear.                    | Code does not run or required input/output is missing.                   |

### **Plagiarism Policy**
* **Copied Code Check:** If the student's code is found to be plagiarized or matching 100% with another student's code, a flat penalty of **3 marks** will be deducted from their total score.